# Firewalls & Packet Filtering 🔥

> **Linux firewalls, stateful filtering, NAT**

## Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      Linux Firewall Evolution                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ipfwadm (Linux 2.0) → ipchains (2.2) → iptables (2.4) → nftables (3.13+) │
│                                                                             │
│  All use Netfilter framework in the kernel                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## Contents

| Topic | File | Description |
|-------|------|-------------|
| iptables | [iptables.md](./iptables.md) | Classic Linux firewall |
| nftables | [nftables.md](./nftables.md) | Modern replacement |
| firewalld | [firewalld.md](./firewalld.md) | RHEL/Fedora frontend |
| ufw | [ufw.md](./ufw.md) | Ubuntu simplified firewall |
| pf | [pf.md](./pf.md) | BSD packet filter |

## Netfilter Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Netfilter Hooks                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                    ┌─────────────────────┐                                 │
│                    │     PREROUTING      │ ← Packet arrives                │
│                    └──────────┬──────────┘                                 │
│                               │                                             │
│                    ┌──────────▼──────────┐                                 │
│                    │   Routing Decision  │                                 │
│                    └──────────┬──────────┘                                 │
│                    ┌──────────┴──────────┐                                 │
│           ┌────────▼────────┐   ┌────────▼────────┐                        │
│           │     INPUT       │   │     FORWARD     │                        │
│           │  (to local)     │   │  (through)      │                        │
│           └────────┬────────┘   └────────┬────────┘                        │
│                    │                     │                                  │
│           ┌────────▼────────┐            │                                 │
│           │  Local Process  │            │                                 │
│           └────────┬────────┘            │                                 │
│                    │                     │                                  │
│           ┌────────▼────────┐            │                                 │
│           │     OUTPUT      │            │                                 │
│           │  (from local)   │            │                                 │
│           └────────┬────────┘            │                                 │
│                    └──────────┬──────────┘                                 │
│                    ┌──────────▼──────────┐                                 │
│                    │    POSTROUTING      │ → Packet leaves                 │
│                    └─────────────────────┘                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## iptables Tables

| Table | Purpose | Chains |
|-------|---------|--------|
| filter | Packet filtering (default) | INPUT, FORWARD, OUTPUT |
| nat | Network Address Translation | PREROUTING, OUTPUT, POSTROUTING |
| mangle | Packet modification | All 5 chains |
| raw | Skip connection tracking | PREROUTING, OUTPUT |
| security | SELinux marking | INPUT, FORWARD, OUTPUT |

## Quick Reference

### iptables

```bash
# List rules
sudo iptables -L -n -v

# Allow SSH
sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT

# Block IP
sudo iptables -A INPUT -s 10.0.0.1 -j DROP

# NAT/Masquerade
sudo iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
```

### nftables

```bash
# List rules
sudo nft list ruleset

# Add rule
sudo nft add rule inet filter input tcp dport 22 accept
```

### firewalld

```bash
# Add service
sudo firewall-cmd --add-service=http --permanent
sudo firewall-cmd --reload
```

### ufw

```bash
# Enable
sudo ufw enable

# Allow port
sudo ufw allow 22/tcp
```

---

## Stateful vs Stateless

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Stateful Firewall                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Connection Tracking States:                                                │
│                                                                             │
│  NEW         - First packet of connection                                  │
│  ESTABLISHED - Part of existing connection                                 │
│  RELATED     - Related to existing (e.g., FTP data, ICMP error)           │
│  INVALID     - Doesn't match any known connection                          │
│                                                                             │
│  Typical stateful rule:                                                    │
│  iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

*Related: [CCNA Security](../CCNA/modules/05-security/) | [CCNP Security](../CCNP/modules/05-security/)*

